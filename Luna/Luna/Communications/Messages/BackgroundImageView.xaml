<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:views="clr-namespace:Luna.Views"
             x:Class="Luna.Communications.Messages.BackgroundImageView"
             Padding="0,8,0,8">
  <ContentView.Content>
        <StackLayout Spacing="0">
            <Label HeightRequest="30" FontAttributes="Italic" VerticalTextAlignment="Center" Padding="10,0,0,0" Text="* Scene Changes *" BackgroundColor="{StaticResource Window Panel Background}" TextColor="White"/>
            <Frame Padding="2" BackgroundColor="{StaticResource Window Panel Background}">
                <views:WidthFittingImage Source="{Binding ImageSrc}"/>
            </Frame>
        </StackLayout>
  </ContentView.Content>
</ContentView>